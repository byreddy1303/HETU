# GO Classes COA Topic Test screenshots

These are exact crops from the learner-provided test-result screenshots dated
2026-08-13.

- `question-qNN.png` omits the result strip and is the only image used by
  practice sessions, attempt receipts, the journal, and spaced reattempts.

Answers, correctness, and timing remain structured database fields and must
never be baked into a reusable question image.

`scripts/build-pyq-bank.mjs` copies this directory to
`public/pyq/images/go-classes-coa-topic-test/` when rebuilding the bank.
